Rebuild the game JS so it runs correctly against the current HTML/CSS and fully respects the task rules. Visual redesign is deferred until you send the chosen UI photos + tech-lead advice — the existing look stays untouched.

## Step 1 — Dictionary (real word list)
- Download the well-known `words_dictionary.json` (~370k English words, the exact file `index.html`'s `data-dictionary` attribute already points to) into the game folder via curl.
- In app.js, fetch that JSON at startup; filter to `^[a-z]+$`, length ≥ 4, minus a profanity blocklist. Since the file is local, the game works offline.
- Build the blocklist empirically: a Node script enumerates every word buildable from each puzzle's letters and flags vulgar entries (preliminary: arse, arses, anal, asses…) so nothing inappropriate is scoreable in a kids' app.
- Clean `dictionary.js` as an emergency fallback (remove the fake words `scarne`, `retrans`, `centa`, `arter`, `arent`, `datal`, `latan`, proper nouns `nepal`/`alps`, obscure junk, duplicates) — used only if the JSON fetch fails, so the game never breaks.

## Step 2 — Rewrite app.js (sync to the current markup, keep the correct logic)
- **Boot**: inject SVG icons into all `[data-icon]` spans; load dictionary; then clear `aria-busy`, enable input/submit/delete/clear/shuffle (currently shipped disabled), update `#dictionary-note` and `#save-status`.
- **Flower**: render 6 outer petals + center as `.wb-petal > .wb-petal-face` positioned via `--x`/`--y` at 60° spacing (percent-based, responsive by CSS — no JS resize handler, no DOM rebuild on resize); center + bonus classes, bonus star badge; click/tap appends a letter.
- **Shuffle**: Fisher-Yates on outer order only — letters, center, and bonus never change; petals glide to new spots via the existing CSS transition.
- **Entry**: `#current-word` is a real input → sync `.value`; typed input filtered to the 7 letters and uppercased; letter-count label; used-letters row; delete/clear buttons; form `submit` with `preventDefault` (fixes the page-reload bug); Enter submits.
- **Validation + scoring** (logic already correct, kept): ≥4 letters, contains center, only the 7 letters, not already submitted, in dictionary; base 2/4/6/12/12+3-per-extra, +5 per bonus-letter occurrence, +7 pangram; invalid submissions never score or consume one of the 12 slots. Feedback wired to the new `data-kind` system: "Too short", "Include the center letter", "Use only the 7 given letters", "Already submitted", "Word not found" + shake animation.
- **Progress panel**: score with pop animation, count x/12, progress-bar width + `aria-valuenow`, accepted-word rows with per-word points, pangram star/highlight, wordbank empty-state toggle.
- **Finish**: at 12 accepted words → finish dialog (final score, pangram count, bonus points earned, best word), confetti (auto-skipped under `prefers-reduced-motion`), "View your results" reopens it; Play again cycles to the next puzzle with a full reset.
- **Extras the HTML ships**: light/dark theme toggle (persisted), sound toggle (WebAudio blips), How-to-play dialog, `?embed=1` demo-shell hiding, keyboard support (letters/Enter/Backspace, ignored while a dialog is open).

## Step 3 — Assets
- Create `assets/favicon.svg` (reuse the inline flower logo mark) so the icon reference resolves.
- Try to download the user-supplied background (URL documented at the top of style.css) into `assets/`; if unreachable, CSS already degrades gracefully to gradients.

## Step 4 — Verification
- `tools/verify.mjs` (Node): asserts each puzzle has ≥12 valid solutions and ≥1 pangram **in the real dictionary**, prints any questionable buildable words for the blocklist, and unit-tests the scoring function against the task table (incl. 8+ letters, repeated bonus letters counting 5 each).
- Browser test via local server: play a full 12-word game, trigger every invalid message, verify shuffle invariance (same letters/center/bonus before/after), check below/at/above the 681px breakpoint in light and dark mode, confirm no horizontal scrolling and all controls usable at ~360px width.